---
- name: Deploy TLS front (Caddy) for the MT5 orchestrator gateway
  hosts: mt5_hosts
  gather_facts: false
  become: true

  vars:
    mt5_gateway_domain: mt5.systematicmindacademy.com
    orchestrator_local: 127.0.0.1:8000
    app_domain: app.systematicmindacademy.com

  tasks:
    - name: Install Caddy signing key
      ansible.builtin.get_url:
        url: https://dl.cloudsmith.io/public/caddy/stable/gpg.key
        dest: /etc/apt/keyrings/caddy.asc
        mode: "0644"

    - name: Add Caddy apt repository
      ansible.builtin.apt_repository:
        repo: >-
          deb [signed-by=/etc/apt/keyrings/caddy.asc]
          https://dl.cloudsmith.io/public/caddy/stable/deb/debian any-version main
        filename: caddy
        state: present

    - name: Install Caddy
      ansible.builtin.apt:
        name: caddy
        state: present
        update_cache: true

    - name: Publish the strategy-lab board
      ansible.builtin.copy:
        src: ../files/strategy-lab-board.html
        dest: /opt/sma/lab/index.html
        mode: "0644"

    - name: Write Caddyfile (auto-HTTPS, reverse proxy to the orchestrator)
      ansible.builtin.copy:
        dest: /etc/caddy/Caddyfile
        mode: "0644"
        content: |
          {{ mt5_gateway_domain }} {
              handle_path /engine/* {
                  reverse_proxy 127.0.0.1:8100
              }
              handle {
                  reverse_proxy {{ orchestrator_local }}
              }
          }

          {{ app_domain }} {
              # Research results board (static, updated by ansible).
              handle_path /lab* {
                  root * /opt/sma/lab
                  file_server
              }
              # Backend API (path preserved: backend serves /api/v1/...).
              handle /api/* {
                  reverse_proxy 127.0.0.1:8080
              }
              handle /docs* {
                  reverse_proxy 127.0.0.1:8080
              }
              handle /openapi.json {
                  reverse_proxy 127.0.0.1:8080
              }
              # Everything else -> the SPA frontend.
              handle {
                  reverse_proxy 127.0.0.1:5173
              }
          }
      register: caddyfile

    - name: Ensure Caddy is enabled and restarted on config change
      ansible.builtin.systemd:
        name: caddy
        enabled: true
        state: "{{ 'restarted' if caddyfile.changed else 'started' }}"

    - name: Wait for HTTPS to answer (Let's Encrypt issuance can take ~30s)
      ansible.builtin.uri:
        url: "https://{{ mt5_gateway_domain }}/api/health"
        status_code: 200
        validate_certs: true
      register: tls_health
      retries: 20
      delay: 6
      until: tls_health.status == 200

    - name: Print TLS gateway summary
      ansible.builtin.debug:
        msg: |-
          MT5 gateway TLS active
          URL: https://{{ mt5_gateway_domain }}
          Health: {{ tls_health.json.status | default('unknown') }}
