---
# Production inventory template.
# Do not commit passwords, API keys, broker credentials, or other secrets here.
all:
  children:
    execution_nodes:
      hosts:
        execution-node-01:
          ansible_host: 194.238.27.144
          ansible_user: administrator
          ansible_connection: winrm
          ansible_winrm_transport: basic
          ansible_winrm_server_cert_validation: ignore
          ansible_port: 5986
